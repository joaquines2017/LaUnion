generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ─── Proveedores ────────────────────────────────────────────────────────────

model Proveedor {
  id            String            @id @default(uuid())
  empresaId     String
  nombre        String
  cuit          String?
  telefono      String?
  email         String?
  direccion     String?
  observaciones String?
  estado        String            @default("activo") // activo | inactivo
  createdAt     DateTime          @default(now())

  empresa Empresa           @relation(fields: [empresaId], references: [id])
  precios PrecioProveedor[]

  @@index([empresaId])
  @@map("proveedores")
}

// ─── Insumos ─────────────────────────────────────────────────────────────────

model CategoriaInsumo {
  id          String   @id @default(uuid())
  empresaId   String
  nombre      String
  descripcion String?

  empresa Empresa  @relation(fields: [empresaId], references: [id])
  insumos Insumo[]

  @@unique([empresaId, nombre])
  @@index([empresaId])
  @@map("categorias_insumo")
}

model Insumo {
  id           String   @id @default(uuid())
  empresaId    String
  codigo       String
  descripcion  String
  categoriaId  String
  unidadMedida String   @default("unidad") // unidad | metro | metroLineal | kilo | par | juego | rollo | placa
  espesormm    Float? // espesor en mm (para placas)
  altoM        Float? // alto estándar de placa en metros
  anchoM       Float? // ancho estándar de placa en metros
  precioBase   Decimal? @db.Decimal(12, 2) // precio fijo para gastos sin proveedor
  estado       String   @default("activo")
  createdAt    DateTime @default(now())

  precioSeleccionadoId String?

  empresa              Empresa            @relation(fields: [empresaId], references: [id])
  categoria            CategoriaInsumo    @relation(fields: [categoriaId], references: [id])
  precios              PrecioProveedor[]  @relation("InsumoPrecios")
  precioSeleccionado   PrecioProveedor?   @relation("PrecioSeleccionado", fields: [precioSeleccionadoId], references: [id])
  despieceMateriales   DespieceMaterial[]
  despieceInsumos      DespieceInsumo[]
  materialesResiduales MaterialResidual[]

  @@unique([empresaId, codigo])
  @@index([empresaId])
  @@index([categoriaId])
  @@index([precioSeleccionadoId])
  @@map("insumos")
}

model PrecioProveedor {
  id            String   @id @default(uuid())
  proveedorId   String
  insumoId      String
  precio        Decimal  @db.Decimal(12, 2)
  fechaVigencia DateTime @default(now())
  estado        String   @default("vigente") // vigente | no_vigente
  creadoEn      DateTime @default(now())
  usuarioId     String?

  proveedor             Proveedor         @relation(fields: [proveedorId], references: [id])
  insumo                Insumo            @relation("InsumoPrecios", fields: [insumoId], references: [id])
  historico             HistorialPrecio[]
  insumoQueLoSelecciona Insumo[]          @relation("PrecioSeleccionado")

  @@unique([proveedorId, insumoId])
  @@map("precios_proveedor")
}

model HistorialPrecio {
  id                String   @id @default(uuid())
  precioProveedorId String
  precioAnterior    Decimal  @db.Decimal(12, 2)
  precioNuevo       Decimal  @db.Decimal(12, 2)
  fechaCambio       DateTime @default(now())
  usuarioId         String

  precioProveedor PrecioProveedor @relation(fields: [precioProveedorId], references: [id])

  @@index([precioProveedorId])
  @@map("historial_precios")
}

// ─── Muebles ─────────────────────────────────────────────────────────────────

model CategoriaMueble {
  id        String   @id @default(uuid())
  empresaId String
  nombre    String

  empresa Empresa  @relation(fields: [empresaId], references: [id])
  muebles Mueble[]

  @@unique([empresaId, nombre])
  @@index([empresaId])
  @@map("categorias_mueble")
}

model Mueble {
  id          String   @id @default(uuid())
  empresaId   String
  codigo      String // carga manual obligatoria, ej: "05-147-000"
  nombre      String
  categoriaId String
  costoActual Decimal  @default(0) @db.Decimal(12, 2)
  estado      String   @default("activo")
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  empresa            Empresa            @relation(fields: [empresaId], references: [id])
  categoria          CategoriaMueble    @relation(fields: [categoriaId], references: [id])
  materiales         DespieceMaterial[]
  insumos            DespieceInsumo[]
  versiones          VersionDespiece[]
  imagenes           MuebleImagen[]
  reservasResiduales ReservaResidual[]

  @@unique([empresaId, codigo])
  @@index([empresaId])
  @@index([categoriaId])
  @@map("muebles")
}

model MuebleImagen {
  id        String   @id @default(uuid())
  muebleId  String
  filename  String
  url       String // ruta pública, ej: /uploads/muebles/<id>/<filename>
  orden     Int      @default(0)
  createdAt DateTime @default(now())

  mueble Mueble @relation(fields: [muebleId], references: [id], onDelete: Cascade)

  @@index([muebleId])
  @@map("mueble_imagenes")
}

// Pestaña "Despiece" — materiales principales (melamina, MDF, vidrio, etc.)
model DespieceMaterial {
  id             String  @id @default(uuid())
  muebleId       String
  insumoId       String? // null si el usuario escribió texto libre
  proveedorId    String? // null = usar precio de referencia (mínimo)
  productoNombre String
  medidas        String? // ej: "90x43", "100x45", "1,50 m"
  cantidad       Decimal @db.Decimal(10, 4)
  costoUnitario  Decimal @db.Decimal(12, 2)
  costoTotal     Decimal @db.Decimal(12, 2)
  orden          Int     @default(0)

  mueble Mueble  @relation(fields: [muebleId], references: [id], onDelete: Cascade)
  insumo Insumo? @relation(fields: [insumoId], references: [id])

  @@index([muebleId])
  @@index([insumoId])
  @@map("despiece_materiales")
}

// Pestaña "Insumos" — insumos varios y gastos (tornillos, flete, etc.)
model DespieceInsumo {
  id            String  @id @default(uuid())
  muebleId      String
  insumoId      String? // null si el usuario escribió texto libre
  descripcion   String
  cantidad      Decimal @db.Decimal(10, 4)
  costoUnitario Decimal @db.Decimal(12, 2)
  costoTotal    Decimal @db.Decimal(12, 2)
  orden         Int     @default(0)

  mueble Mueble  @relation(fields: [muebleId], references: [id], onDelete: Cascade)
  insumo Insumo? @relation(fields: [insumoId], references: [id])

  @@index([muebleId])
  @@index([insumoId])
  @@map("despiece_insumos")
}

model VersionDespiece {
  id                 String   @id @default(uuid())
  muebleId           String
  numeroVersion      Int
  fecha              DateTime @default(now())
  usuarioId          String
  motivo             String?
  snapshotMateriales Json // snapshot completo de DespieceMaterial[]
  snapshotInsumos    Json // snapshot completo de DespieceInsumo[]

  mueble Mueble @relation(fields: [muebleId], references: [id])

  @@index([muebleId])
  @@map("versiones_despiece")
}

// ─── Empresas (Multi-Tenancy) ─────────────────────────────────────────────────

model Empresa {
  id        String   @id @default(uuid())
  nombre    String
  logoUrl   String?
  dominio   String? // informativo: "mueblesm.ddns.net"
  estado    String   @default("activo") // activo | inactivo
  createdAt DateTime @default(now())

  usuarios             Usuario[]
  proveedores          Proveedor[]
  categoriasInsumo     CategoriaInsumo[]
  categoriasMueble     CategoriaMueble[]
  unidadesMedida       UnidadMedida[]
  insumos              Insumo[]
  muebles              Mueble[]
  materialesResiduales MaterialResidual[]
  configuracion        ConfiguracionGlobal?
  logsAuditoria        LogAuditoria[]

  @@map("empresas")
}

// ─── Usuarios y Seguridad ────────────────────────────────────────────────────

model Usuario {
  id            String   @id @default(uuid())
  nombreUsuario String   @unique
  email         String   @unique
  passwordHash  String // bcrypt hash
  rol           String   @default("operador") // superadmin | administrador | operador | lectura
  estado        String   @default("activo")
  createdAt     DateTime @default(now())

  empresaId String?
  empresa   Empresa? @relation(fields: [empresaId], references: [id])

  @@index([empresaId])
  @@map("usuarios")
}

// ─── Catálogos ───────────────────────────────────────────────────────────────

model UnidadMedida {
  id          String  @id @default(uuid())
  empresaId   String
  nombre      String // "unidad", "metro", "placa", etc.
  descripcion String?

  empresa Empresa @relation(fields: [empresaId], references: [id])

  @@unique([empresaId, nombre])
  @@index([empresaId])
  @@map("unidades_medida")
}

// ─── Materiales Residuales ───────────────────────────────────────────────────

model MaterialResidual {
  id        String   @id @default(uuid())
  empresaId String
  insumoId  String
  altoCm    Float
  anchoCm   Float
  cantidad  Int      @default(1)
  nota      String?
  estado    String   @default("disponible") // disponible | usado
  createdAt DateTime @default(now())

  empresa  Empresa           @relation(fields: [empresaId], references: [id])
  insumo   Insumo            @relation(fields: [insumoId], references: [id])
  reservas ReservaResidual[]

  @@index([empresaId])
  @@index([insumoId])
  @@map("materiales_residuales")
}

// ─── Reservas de Residuales ──────────────────────────────────────────────────

model ReservaResidual {
  id                 String   @id @default(uuid())
  materialResidualId String
  muebleId           String
  cantidadAsignada   Int      @default(1) // retazos de este material asignados a este mueble
  createdAt          DateTime @default(now())

  materialResidual MaterialResidual @relation(fields: [materialResidualId], references: [id], onDelete: Cascade)
  mueble           Mueble           @relation(fields: [muebleId], references: [id], onDelete: Cascade)

  @@unique([materialResidualId, muebleId]) // un retazo = una entrada por mueble
  @@map("reservas_residual")
}

// ─── Configuración y Auditoría ───────────────────────────────────────────────

model ConfiguracionGlobal {
  id                 String  @id @default("1") // singleton (RFF-002 lo migra a config por empresa)
  empresaId          String? @unique
  factorDesperdicio  Float   @default(1.10) // 1.10 = 10% desperdicio por defecto
  moneda             String  @default("ARS")
  vigenciaPrecioDias Int     @default(30)

  empresa Empresa? @relation(fields: [empresaId], references: [id], onDelete: SetNull)

  @@map("configuracion_global")
}

model LogAuditoria {
  id              String   @id @default(uuid())
  empresaId       String?
  fechaHora       DateTime @default(now())
  usuarioId       String
  accion          String // PRECIO_MODIFICADO | DESPIECE_MODIFICADO | RECALCULO_CASCADA | etc.
  entidad         String // Mueble | Insumo | PrecioProveedor | etc.
  entidadId       String
  datosAnteriores Json?
  datosNuevos     Json?

  empresa Empresa? @relation(fields: [empresaId], references: [id], onDelete: SetNull)

  @@index([empresaId])
  @@map("log_auditoria")
}
